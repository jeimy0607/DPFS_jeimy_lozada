<!DOCTYPE html>
<html lang="es">
<head>
  <% include ./partials/head %>
</head>
<body>
  <%- include('./partials/header') %>

  <main class="registro-caja">
    <h1>Recuperar contraseña</h1>

    <% if (error) { %>
      <p class="error-msg"><%= error %></p>
    <% } %>

    <% if (msg) { %>
      <p><%= msg %></p>
    <% } %>

    <% if (link) { %>
      <p>
        <a class="btn" href="<%= link %>">Ir al link de recuperación</a>
      </p>
      <p style="font-size:12px; opacity:.8;">(Simulación: en la vida real esto llegaría al correo)</p>
    <% } %>

    <form action="/recuperarcontrasena" method="POST" class="form">
      <div class="campo">
        <label for="email">Correo</label>
        <input id="email" name="email" type="email" required>
      </div>
      <button class="btn" type="submit">Generar link</button>
    </form>
  </main>

  <%- include('./partials/footer') %>
</body>
</html>
